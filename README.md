# PassManager
A full-stack Password Manager web application built with the MERN stack that allows users to securely store and manage their credentials. It features a responsive UI, favicon fetching from URLs, and both local and MongoDB-based storage options.
